{
  "name": "realflow-ai-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Provider-agnostic AI agent that drives RealFlow via validated JSON operations (GLM / Gemini / Anthropic).",
  "scripts": {
    "generate": "node generate.mjs"
  },
  "dependencies": {
    "@realflow/core": "0.1.0"
  }
}
